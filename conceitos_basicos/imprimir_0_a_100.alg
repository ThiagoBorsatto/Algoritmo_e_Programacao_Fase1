algoritmo "imprimir_0_a_100"
// Escreva um algoritmo para imprimir os números de 0 (inclusive) a 100 (inclusive) em ordem decrescente.

var
   contador:inteiro
inicio
   contador <- 100
   enquanto (contador >= 0) faca
      escreval(contador)
      contador <- contador - 1
   fimenquanto
fimalgoritmo
