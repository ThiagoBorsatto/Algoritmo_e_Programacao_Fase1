algoritmo "n_pares_0_a_100"
// Escreva um algoritmo que escreva na tela os números pares de 0 (inclusive) a 100 (inclusive).

var
   contador:inteiro
inicio
   contador <- 0
   enquanto (contador <= 100) faca
      escreval(contador)
      contador <- contador + 2
   fimenquanto
fimalgoritmo
